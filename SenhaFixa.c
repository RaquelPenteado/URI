#include <stdio.h>

int main() {
    int cont,senha;


    for (cont = 0; cont < senha != 2002 ; cont++) {
        scanf("%i",&senha);
        if(senha != 2002){
            printf("Senha Invalida\n");
            //scanf("%i", &senha);
        } else if(senha == 2002) {
            printf("Acesso Permitido\n");
            return 0;
        }
    }
}